#include<vector>
#include<string>

using std::string;
using std::vector;

int main()
{
    vector<int> p;
    // c++11 standard list initializer
    vector<string> svc = {"A", "B", "C"};
    vector<vector<string>> file;
    return 0;
}
