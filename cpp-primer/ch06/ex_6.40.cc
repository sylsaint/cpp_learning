#include<iostream>

using namespace std;

int ff(int a, int b = 0, int c = 10);
char *init(int ht = 10, int wd, char backgrnd);
// error: if a parameter has a default argument, all the parameters that follow it must also have default arguments.

int main()
{
    return 0;
}
